apiVersion: v1
kind: Service
metadata:
  name: chai-app-service
  namespace: default

spec:
  selector:
    app: chai-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: ClusterIP
